# Orders — on standby

The working order demo is preserved from commit f3ad84f. These authored files and
tests are a restoration snapshot, not the active application or active test suite.
Its complete repository state remains available with `git show f3ad84f:<path>`.
No grid or new component has been introduced. Restore it as a later Rosetta example
behind the shared HTML frame, removing its old comparison navigation from app code.
The old browser tests must then target that example inside the frame.
