{
  "name": "demo-rosetta-checks",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "metrics": "python3 scripts/measure.py",
    "build:editor": "esbuild shared/editor/editor.js --bundle --format=esm --outfile=shared/editor/dist/editor.js"
  },
  "devDependencies": {
    "@codemirror/lang-javascript": "^6.2.5",
    "@codemirror/theme-one-dark": "^6.1.3",
    "@playwright/test": "1.63.0",
    "codemirror": "^6.0.2",
    "esbuild": "^0.28.2"
  }
}
